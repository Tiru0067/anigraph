/**
 * AniList GraphQL Client Utility
 * (Will fetch anime, staff, characters, studios and relations from AniList API)
 */

export const fetchAniListData = async (page = 1, perPage = 50) => {
  // Placeholder for seed data ingestion
  return [];
};

export default {
  fetchAniListData
};
